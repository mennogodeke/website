<div class="cv">

  <section class="cv-section">
    <h1 class="cv-name">Menno Godeke</h1>
    <p class="cv-tagline">Freelance Software Engineer</p>
  </section>

  <section class="cv-section">
    <h2 class="cv-section-title">Experience</h2>

    <% @jobs.each do |job| %>
      <div class="cv-entry">
        <div class="cv-entry-header">
          <div>
            <span class="cv-entry-title"><%= job.title %></span>
            <span class="cv-entry-company"><%= job.company %></span>
          </div>
          <span class="cv-entry-dates">
            <%= job.start_date.strftime("%b %Y") %> – <%= job.end_date ? job.end_date.strftime("%b %Y") : "Present" %>
          </span>
        </div>
        <% if job.description.present? %>
          <p class="cv-entry-description"><%= job.description %></p>
        <% end %>
        <% if job.skills.any? %>
          <div class="cv-entry-tags">
            <% job.skills.each do |skill| %>
              <span class="tag"><%= skill.name %></span>
            <% end %>
          </div>
        <% end %>
      </div>
    <% end %>
  </section>

  <section class="cv-section">
    <h2 class="cv-section-title">Expertise</h2>

    <% @expertises.each do |expertise| %>
      <div class="cv-entry">
        <div class="cv-entry-header">
          <span class="cv-entry-title"><%= expertise.name %></span>
          <span class="cv-entry-dates"><%= expertise.years_of_experience %>+ years</span>
        </div>
        <% if expertise.description.present? %>
          <p class="cv-entry-description"><%= expertise.description %></p>
        <% end %>
      </div>
    <% end %>
  </section>

  <section class="cv-section">
    <h2 class="cv-section-title">Skills</h2>

    <div class="cv-skills">
      <% @skills.group_by(&:level).sort_by { |level, _| -Skill.levels[level] }.each do |level, skills| %>
        <div class="cv-skill-group">
          <span class="cv-skill-level"><%= level.capitalize %></span>
          <span class="cv-skill-names"><%= skills.map(&:name).join(", ") %></span>
        </div>
      <% end %>
    </div>
  </section>

</div>
