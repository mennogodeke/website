<a href="<%= project_path(project) %>" class="proj-row <%= index.odd? ? 'flipped' : '' %>">
  <div class="proj-content">
    <p class="font-mono text-xs mb-2"><%= project.year %></p>
    <h3 class="mb-2"><%= project.name %></h3>
    <p class="mb-4"><%= project.description %></p>
    <div class="tags mb-4">
      <% project.skills.each do |skill| %>
        <span class="tag"><%= skill.name %></span>
      <% end %>
    </div>
    <div style="display:flex; align-items:center; gap:1rem;">
      <span class="proj-cta">View case study →</span>
      <% if project.url.present? %>
        <span class="font-mono text-xs">↗ <%= project.url %></span>
      <% end %>
      <% if project.repo_url.present? %>
        <span class="font-mono text-xs">↗ repo</span>
      <% end %>
    </div>
  </div>
  <div class="proj-img img-stub">
    <span class="font-mono text-xs"><%= project.name %> screenshot</span>
  </div>
</a>
